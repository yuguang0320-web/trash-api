services:
  - type: web
    name: trash-recognition-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false   # You will fill this in on Render dashboard
    plan: free
